# FleetFlow - Variables de Entorno para Netlify
# Copia este archivo como referencia para configurar las variables en Netlify UI

# =============================================================================
# SUPABASE CONFIGURATION (OBLIGATORIO)
# =============================================================================
# Obtener de: Supabase Dashboard > Settings > API
# URL de tu proyecto Supabase
VITE_SUPABASE_URL=https://tu-proyecto.supabase.co

# Clave anonima de Supabase (anon/public key)
VITE_SUPABASE_ANON_KEY=tu_clave_anonima_aqui

# =============================================================================
# GOOGLE OAUTH CONFIGURATION (OBLIGATORIO)
# =============================================================================
# Obtener de: Google Cloud Console > APIs & Services > Credentials
# Client ID de OAuth 2.0
VITE_GOOGLE_CLIENT_ID=tu-client-id.apps.googleusercontent.com

# =============================================================================
# CONFIGURACION OPCIONAL
# =============================================================================

# Gemini AI para funcionalidades de chat IA (opcional)
VITE_GEMINI_API_KEY=tu_gemini_api_key

# Google Maps para funcionalidades de mapa (opcional)
VITE_GOOGLE_MAPS_API_KEY=tu_google_maps_api_key

# Entorno de ejecucion
VITE_ENV=production

# Google Analytics (opcional)
VITE_GA_TRACKING_ID=tu_google_analytics_id

# =============================================================================
# INSTRUCCIONES PARA NETLIFY
# =============================================================================
# 1. Ve a tu proyecto en Netlify Dashboard
# 2. Settings > Build & deploy > Environment
# 3. Click en "Add a variable"
# 4. Agrega cada variable (nombre y valor)
# 5. NO incluyas comillas en los valores
# 6. OBLIGATORIO: VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_GOOGLE_CLIENT_ID
# 7. Deploy > Trigger deploy para reconstruir con las nuevas variables